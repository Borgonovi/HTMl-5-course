function multiplay (a, b) {
	a * b
};

var test = function(){

};
	
sendEmail('fe.borgonovi@gmail.com', callback)

sendEmail = function(dest, cb){
	email.on('sended', function(err, data){
		cb(err, data);
	});
}
