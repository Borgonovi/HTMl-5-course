form(ng-submit='searchForm.$valid && search.submit()',
name='searchForm', novalidate)
	md-input-container
		label Github Search
		input(type='text', ng-model='search.value', required)

	md-button.md-raised Search

	div(ng-repeat='repo in search.results') {{repo.full_name}}